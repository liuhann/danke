import 'ant-design-vue/dist/antd.css'

import { FormModel, Upload, Button, Slider, Input } from 'ant-design-vue'

export default {
  components: {
    FormModel,
    Upload,
    Button,
    Input,
    Slider
  }
}
